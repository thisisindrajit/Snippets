export type TSnippet = {
  what: string;
  why: string;
  when: string;
  where: string;
  how: string;
  amazingfacts?: string[];
  abstract?: string;
  abstract_embedding?: number[];
  tags?: string[];
};
